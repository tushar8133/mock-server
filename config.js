const PORT = 3000;

const HAR_FILE = "8888";

const ALLOW_MERGE = true;

const VERBOSE = true;

const DEFAULT_PATH = "./";

const RESPONSE_DELAY = 0;

module.exports = {
    PORT,
    HAR_FILE,
    ALLOW_MERGE,
    VERBOSE,
    DEFAULT_PATH,
    RESPONSE_DELAY
}